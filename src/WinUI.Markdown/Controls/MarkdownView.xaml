<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="WinUI.Markdown.Controls.MarkdownView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Grid x:Name="PART_LayoutRoot">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="PART_EditorColumn" Width="0" />
            <ColumnDefinition x:Name="PART_SplitterColumn" Width="0" />
            <ColumnDefinition x:Name="PART_PreviewColumn" Width="*" />
        </Grid.ColumnDefinitions>

        <Border
            x:Name="PART_EditorHost"
            Grid.Column="0"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="0,0,1,0"
            Visibility="Collapsed" />

        <Border
            x:Name="PART_Splitter"
            Grid.Column="1"
            Background="Transparent"
            Visibility="Collapsed"
            Width="8">
            <Rectangle
                Width="2"
                Height="24"
                HorizontalAlignment="Center"
                VerticalAlignment="Center"
                RadiusX="1"
                RadiusY="1"
                Fill="{ThemeResource TextFillColorTertiaryBrush}" />
        </Border>

        <Grid
            x:Name="PART_Root"
            Grid.Column="2" />
    </Grid>
</UserControl>
